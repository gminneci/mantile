# HuggingFace Token for accessing gated models
# Get your token from: https://huggingface.co/settings/tokens
# Request access to gated models (e.g., Llama) on their model pages
HF_TOKEN=your_token_here

# Deployment Configuration
# ========================

# Backend API path prefix (e.g., /estimator-api for reverse proxy deployment)
# Leave empty for root path deployment
ROOT_PATH=

# Frontend base path (e.g., /estimator/ for reverse proxy deployment)
# Leave empty for root path deployment
VITE_BASE_PATH=/

# Backend API URL for frontend to connect to
# For production, set this to your backend URL (e.g., https://your-domain.com/estimator-api)
VITE_API_URL=http://localhost:8000

# CORS allowed origins (comma-separated)
# For production, add your frontend URL (e.g., https://your-domain.com,https://your-domain.com/estimator)
CORS_ORIGINS=http://localhost:5173
